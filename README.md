### Tom Weisz — S2M Consulting, Paris

I build and run small production systems, end to end: the API, the database, the
worker, the deploy, and the alert that fires when it breaks at 3am.

Most of my work lives in private client repos. The public ones here are the
patterns I reuse — stripped of client data, kept honest, kept running.

**Working on**

- **TxLens** — on-chain forensics for crypto fraud cases: wallet tracing, flow
  reconstruction, reports for lawyers. Telegram bot + Pro tier.
- **Comptao** — an AI collaborator for accounting firms.
- **Artisan 360** — quotes and invoicing for building trades, as a PWA.
- **SalesAuto** — multi-agent B2B outreach, from list building to send.

**Stack I actually ship with**

`Python` `FastAPI` `Pydantic v2` `SQLModel / SQLAlchemy 2.0 async` `PostgreSQL`
`Redis / ARQ` `Docker` `Caddy` `Prometheus` `n8n` `LLM agents in production`
`Next.js` `TypeScript`

**How I work**

Fixed scope, fixed price, a repo you own at the end, and a written handover so the
next person is not archaeology. I automate aggressively — that is why small projects
land fast and stay cheap.

📍 Paris · 🌍 remote, async · ✉️ tom@s2mconsulting.fr · 🔗 [s2mconsulting.fr](https://s2mconsulting.fr)
